# 项目协作规则

## 新功能必须先生成设计稿

当用户提出新增功能需求时，必须依次完成以下流程：

1. 查看当前项目相关页面、组件、样式和已有设计资产，归纳现有布局、配色、字体、间距及交互风格。
2. 在编写该功能的实现代码之前，使用生图工具生成符合当前项目设计风格的设计稿。遵循适用的 imagegen 技能；不能用文字描述、线框代码或直接实现页面代替生图。
3. 将设计稿保存到项目的 `design-mockups/` 目录，使用能识别功能和版本的文件名，并向用户展示或提供文件链接。
4. 根据设计稿实现功能，复用现有组件、样式和真实业务能力；适配移动端、可访问性、加载及异常状态。
5. 完成后检查实际页面与设计稿的一致性，并验证主要功能。记录必要的实现差异。

除非用户明确要求先审批，否则生成设计稿后继续实现，不额外增加确认环节。如生图工具不可用，应说明阻塞原因，不得声称已生成设计稿或悄悄跳过该步骤。

这条流程适用于新增功能；现有功能的普通修复或仅调整文案不自动视为新增功能。用户在当前任务中给出的明确指令优先。

## 商业产品设计标准

- 3D 和核心功能应自动初始化，首屏立即展示产品预览图；禁止让用户点击 “Load interactive 3D” 等技术加载入口。加载过程简洁，只有失败时才显示重试。
- 整个页面都要经过 UI 设计，包括首屏、操作区、内容区、空状态、错误状态和移动端，不能只设计首屏后拼接通用模板。
- 这是正式商业网站。避免 AI 演示页感：禁止泛泛口号、重复营销段落、无意义装饰、虚构数据与能力、面向用户的实现术语。以清晰产品信息、真实素材、精确排版和一致交互为准。

- 工具页按一屏一个主题组织模块，给予充分留白。桌面端每个主要模块至少占一屏可用高度；移动端内容可自然超出一屏，不通过裁切或过度缩小字号强行塞入。
